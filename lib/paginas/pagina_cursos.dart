import 'package:flutter/material.dart';

class PaginaCursos extends StatelessWidget {
  const PaginaCursos({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Text('Página de cursos');
  }
}